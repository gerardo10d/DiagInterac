import numpy as np
import math
# nBarrasb = 3
# nBarrash = 4
#
# array = np.ones(nBarrash) * 2
# array[0], array[-1] = nBarrasb, nBarrasb
# print(array)
# M, P = np.zeros(3), np.zeros(3)
# print(M, P)
x = (290 + 290) / 3
print(x/2)